# first execution
var1 = 1
# second execution
print(var1)
# error execution, this error do not affect the command before it
print(var2)

# you should notice that python execute command line by line without pre-compilation!